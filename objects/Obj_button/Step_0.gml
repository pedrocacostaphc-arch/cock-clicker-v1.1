/*
if mouse_check_button_pressed(mb_left){

	if (global.point >= buy) {
		
        global.point -= buy; // Cobra o preço fixo de 10 pontos
        global.cliques *= 2;       // Dobra o valor do clique para sempre
		buy *= 2;
    }
}
*/
